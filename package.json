{
  "name": "md2jsx",
  "version": "0.0.1",
  "description": "Build-time Markdown-to-JSX conversion",
  "scripts": {
    "test": "mocha --require ts-node/register src/*.spec.ts"
  },
  "dependencies": {
    "@types/marked": "^4.0.7",
    "marked": "^4.1.1"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "^18.8.5",
    "mocha": "^10.0.0",
    "ts-node": "^10.9.1"
  }
}
